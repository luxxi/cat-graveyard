<%= month_calendar(events: @reservations, attribute: :start_on, end_attribute: :end_on) do |date, reservations| %>
  <%= date.strftime('%e %b') %>
  <div>
    <%= link_to pluralize(reservations.count, 'reservation'), "" unless reservations.count == 0 %>
    <div>
    </div>
  </div>
<% end %>
